while read path
do
	dir=${path%/*}
	echo "-----------------------"
	echo $path;
	if [ -e ${path} ]; then
		echo $path;
		if [ ! -e .${dir} ]; then
			sudo mkdir -p .$dir
		fi
		sudo cp -upv $path .$path
	fi
done <list
